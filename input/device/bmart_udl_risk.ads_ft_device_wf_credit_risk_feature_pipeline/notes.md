# Case Notes

- Case name: `bmart_udl_risk.ads_ft_device_wf_credit_risk_feature_pipeline`
- Source code copied from the old repository's `schedule/pipeline`, `features/device`, and `dao/device` files.
- Spark UI export copied from the old skill input folder and kept with its original HTML/resource layout.
- Use `source/` as the working copy and `source.zip` as the raw package.
