# 第 5 步：代码变更

## 目的

只执行用户已经批准的优化。

## 输入

- Approved optimization items
- Current source files

## 流程

1. 只改最小必要的代码面。
2. 除非优化明确要求改变行为，否则保持行为一致。
3. 保持可读、可审查。
4. 把修改后的路径再和原瓶颈核一遍。
5. 如果修复会碰到多个热点，只有在它们共享同一个根因时才合并处理。

## 输出

- 更新后的代码
- 简短变更摘要
- 仍未解决的问题（如有）
- 修改路径的验证说明
